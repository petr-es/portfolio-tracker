name: Update prices

on:
  schedule:
    # 10:00 Prague (CEST = UTC+2, summer) / 09:00 (CET = UTC+1, winter)
    - cron: '0 8 * * 1-5'
    # 12:30 Prague (CEST) / 11:30 (CET)
    - cron: '30 10 * * 1-5'
    # 15:00 Prague (CEST) / 14:00 (CET)
    - cron: '0 13 * * 1-5'
    # 18:00 Prague (CEST) / 17:00 (CET)
    - cron: '0 16 * * 1-5'
  workflow_dispatch: # allow manual trigger

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install yfinance pytz

      - name: Fetch prices and update data.js
        run: python scripts/update_prices.py

      - name: Commit and push if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add scripts/prices.js history.js
          git diff --staged --quiet || git commit -m "chore: update prices $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
